version: '2'
services:
  web:
    build: .
    command: npm start
    volumes:
      - .:/usr/app/
      - /usr/app/node_modules
    ports:
      - "3000:3000"
    depends_on:
      - postgres
    env_file:
      - .env
  postgres:
    image: postgres:9.6.2-alpine
    env_file:
      - .env